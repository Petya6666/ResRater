<UserControl x:Class="resrater_adminfelulet.Views.KommentekView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="💬 Kommentek kezelése" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,12"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource SearchBoxStyle}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1" Content="🔍 Keresés" Command="{Binding SearchCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
            </Grid>

            <Button Grid.Column="3" Content="🔄 Frissítés" Command="{Binding LoadCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <Button Grid.Column="5" Content="🗑️ Törlés" Command="{Binding DeleteCommand}"
                    Style="{StaticResource DangerButtonStyle}"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Kommentek}"
                  SelectedItem="{Binding SelectedKomment}"
                  Style="{StaticResource AdminDataGridStyle}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding KommentId}" Width="60"/>
                <DataGridTextColumn Header="Felhasználó" Binding="{Binding FelhasznaloNev}" Width="130"/>
                <DataGridTextColumn Header="Étterem" Binding="{Binding EtteremNev}" Width="150"/>
                <DataGridTextColumn Header="Megjegyzés" Binding="{Binding Megjegyzes}" Width="*"/>
                <DataGridTextColumn Header="Dátum" Binding="{Binding LetrehozIdo, StringFormat=yyyy.MM.dd HH:mm}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="11" VerticalAlignment="Center"/>
            <ProgressBar Grid.Column="1" IsIndeterminate="True" Width="100" Height="4"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibleConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
