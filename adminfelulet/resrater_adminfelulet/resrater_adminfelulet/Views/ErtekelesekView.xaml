<UserControl x:Class="resrater_adminfelulet.Views.ErtekelesekView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="⭐ Értékelések kezelése" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,12"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Összes értékelés listája" VerticalAlignment="Center"
                       Foreground="Gray" FontStyle="Italic"/>
            <Button Grid.Column="2" Content="🔄 Frissítés" Command="{Binding LoadCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <Button Grid.Column="4" Content="🗑️ Törlés" Command="{Binding DeleteCommand}"
                    Style="{StaticResource DangerButtonStyle}"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Ertekelesek}"
                  SelectedItem="{Binding SelectedErtekeles}"
                  Style="{StaticResource AdminDataGridStyle}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ErtekelesId}" Width="60"/>
                <DataGridTextColumn Header="Étterem" Binding="{Binding EtteremNev}" Width="160"/>
                <DataGridTextColumn Header="Felhasználó" Binding="{Binding FelhasznaloNev}" Width="120"/>
                <DataGridTextColumn Header="Átlag" Binding="{Binding Atlag, StringFormat=0.00}" Width="80"/>
                <DataGridTextColumn Header="Ételminőség" Binding="{Binding Etelminoseg}" Width="100"/>
                <DataGridTextColumn Header="Kiszolgálás" Binding="{Binding Kiszolgalas}" Width="100"/>
                <DataGridTextColumn Header="Hangulat" Binding="{Binding Hangulat}" Width="90"/>
                <DataGridTextColumn Header="Dátum" Binding="{Binding Datum, StringFormat=yyyy.MM.dd HH:mm}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="11" VerticalAlignment="Center"/>
            <ProgressBar Grid.Column="1" IsIndeterminate="True" Width="100" Height="4"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibleConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
